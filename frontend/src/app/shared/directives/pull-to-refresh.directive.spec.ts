import { PullToRefreshDirective } from './pull-to-refresh.directive';

describe('PullToRefreshDirective', () => {
  it('should create an instance', () => {
    const directive = new PullToRefreshDirective();
    expect(directive).toBeTruthy();
  });
});
